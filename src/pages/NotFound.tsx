import React from 'react';

const NotFound = () => {
    return <span>You've tried to access a page that doesn't exist. To return to the home page, click on "Home" at the top.</span>
  }
  
export default NotFound;